<Window x:Class="VectorPilot.App.Controls.GadgetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Run Lua Gadget" Height="520" Width="720"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Example:" VerticalAlignment="Center" Foreground="#555"/>
            <ComboBox x:Name="CmbExample" Width="200" Margin="6,0,0,0"
                      SelectionChanged="Example_Changed"/>
            <TextBlock Text="Sandboxed — no file, shell, or network access."
                       VerticalAlignment="Center" Margin="14,0,0,0" Foreground="#888" FontSize="11"/>
        </StackPanel>

        <TextBox x:Name="TxtScript" Grid.Row="1" AcceptsReturn="True" AcceptsTab="True"
                 FontFamily="Consolas" FontSize="12" VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"/>

        <TextBlock x:Name="TxtResult" Grid.Row="2" Margin="0,8,0,0" TextWrapping="Wrap"
                   FontSize="11" Foreground="#555"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnRun" Content="Run" Width="90" Height="28" Click="Run_Click" IsDefault="True"/>
            <Button x:Name="BtnAdd" Content="Add to sheet" Width="110" Height="28" Margin="8,0,0,0"
                    Click="Add_Click" IsEnabled="False"/>
            <Button Content="Close" Width="80" Height="28" Margin="8,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
