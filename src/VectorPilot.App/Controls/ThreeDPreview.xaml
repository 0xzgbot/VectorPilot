<UserControl x:Class="VectorPilot.App.Controls.ThreeDPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="640">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Viewport3D x:Name="Viewport" ClipToBounds="True">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="Camera" Position="120, -160, 140" LookDirection="-120, 160, -140"
                                   UpDirection="0, 0, 1" FieldOfView="45" />
            </Viewport3D.Camera>
            <Viewport3D.Children>
                <ModelVisual3D>
                    <ModelVisual3D.Content>
                        <DirectionalLight Color="White" Direction="-1, -2, -3" />
                    </ModelVisual3D.Content>
                </ModelVisual3D>
                <ModelVisual3D x:Name="StockModel" />
                <ModelVisual3D x:Name="ToolpathModel" />
            </Viewport3D.Children>
        </Viewport3D>
        <Border Grid.Row="1" Background="#FF1C1C1C" Padding="8,6">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnPlay" Content="▶" Width="34" Click="Play_Click"/>
                <Button x:Name="BtnStep" Content="⏭" Width="34" Margin="4,0,0,0" Click="Step_Click"/>
                <Button x:Name="BtnRestart" Content="⏮" Width="34" Margin="4,0,0,0" Click="Restart_Click"/>
                <TextBlock x:Name="TxtProgress" Text="0%" Foreground="#FFCCCCCC" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12"/>
                <TextBlock Text="Speed" Foreground="#FFCCCCCC" VerticalAlignment="Center" Margin="14,0,0,0" FontSize="12"/>
                <Slider x:Name="SpeedSlider" Width="110" Minimum="1" Maximum="16" Value="2"
                        TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Margin="6,0,0,0"
                        ValueChanged="SpeedSlider_ValueChanged"/>
                <TextBlock x:Name="TxtSpeed" Text="2×" Foreground="#FFCCCCCC" VerticalAlignment="Center" Margin="6,0,0,0" FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
