<Window x:Class="VectorPilot.App.Controls.PreferencesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preferences" Width="420" Height="380" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Units"/>
        <ComboBox x:Name="CmbUnits" Grid.Row="1" Margin="0,2,0,10">
            <ComboBoxItem Content="mm" Tag="mm"/>
            <ComboBoxItem Content="inch" Tag="inch"/>
        </ComboBox>

        <TextBlock Text="Theme" Grid.Row="2"/>
        <ComboBox x:Name="CmbTheme" Grid.Row="3" Margin="0,2,0,10">
            <ComboBoxItem Content="Dark" Tag="Dark"/>
            <ComboBoxItem Content="Light" Tag="Light"/>
        </ComboBox>

        <TextBlock Text="Autosave interval (seconds)" Grid.Row="4"/>
        <TextBox x:Name="TxtAutosave" Grid.Row="5" Margin="0,2,0,10"/>

        <CheckBox x:Name="ChkGrid" Grid.Row="6" Content="Show grid" VerticalAlignment="Top" Margin="0,6,0,0"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="6" VerticalAlignment="Bottom">
            <Button Content="Save" Width="80" Click="Save_Click"/>
            <Button Content="Cancel" Width="80" Margin="8,0,0,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
