<Window x:Class="VectorPilot.App.Controls.MachineConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Machine Configuration" Width="560" Height="440" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ListBox x:Name="MachineList" SelectionChanged="MachineList_SelectionChanged"/>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,6,0,0">
                <Button Content="Use This Machine" Padding="8,4" Click="Use_Click"/>
                <Button Content="Delete" Margin="6,0,0,0" Padding="8,4" Click="Delete_Click"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="2">
            <StackPanel>
                <TextBlock Text="Name"/>
                <TextBox x:Name="TxtName"/>
                <TextBlock Text="Travel X (mm)" Margin="0,8,0,0"/>
                <TextBox x:Name="TxtX"/>
                <TextBlock Text="Travel Y (mm)" Margin="0,8,0,0"/>
                <TextBox x:Name="TxtY"/>
                <TextBlock Text="Travel Z (mm)" Margin="0,8,0,0"/>
                <TextBox x:Name="TxtZ"/>
                <TextBlock Text="Axes" Margin="0,8,0,0"/>
                <TextBox x:Name="TxtAxes"/>
                <TextBlock Text="Port" Margin="0,8,0,0"/>
                <TextBox x:Name="TxtPort"/>
                <Button Content="Save Machine" Margin="0,14,0,0" Click="Save_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
